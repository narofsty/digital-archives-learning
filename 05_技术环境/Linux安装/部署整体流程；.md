1.检查jdk, 解压安装jdk,

```
java -version

```
2.安装docker,tomcat,配置自启动，

3.通过docker安装按钻过mysql'l'j

3.安装 mysql,
mysqllj
4.reids

5.kkfile
6.ocr 

docker ps 
查看服务是否启动

将打包好的war 包放在下面路径下

```
/usr/local/soft/apache-tomcat-9.0.72/webapps
```
启动tomcat  /关闭tomcat

`startup.sh     shutdown.sh`

追踪日志，查看是否启动成功
`tail -f ./logs/catalina.out`

修改配置文件
application.yml     application-dev